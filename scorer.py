def judge(question, expects, answer, results):
    """Placeholder scorer used until the student implements the real rubric.

    The evaluation script imports this function and calls it for each run.
    Returning True keeps the run log valid while leaving the actual grading
    logic to be filled in later.
    """
    return True